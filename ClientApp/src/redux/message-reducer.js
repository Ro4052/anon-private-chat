// const SEND_MESSAGE = "SEND_MESSAGE";

const initialState = {
  userName: []
};

export default function reducer(state = initialState, action = {}) {
  switch (action.type) {
    default: {
      return state;
    }
  }
}
